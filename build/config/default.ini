config.host.name = server
config.base.url = opm.local

; General config
config.locale = en
config.secret = ThisTokenIsNotSoSecretChangeIt
config.router.http_port = 80
config.router.https_port = 443

; Default DATABASE CONNECTION PARAMETERS
config.database.default.driver = pdo_mysql

; Default DATABASE CONNECTION PARAMETERS
config.database.default.host = localhost
config.database.default.port =
config.database.default.user = root
config.database.default.password =
config.database.default.name = opm-server

; Mailer config
config.mailer.transport = mail
config.mailer.host =
config.mailer.user =
config.mailer.password =

; Log Path  for the Application used in AppKernel_dev and written on build to AppKernel
config.path.logs = ${project.basedir}/app/logs/${build.revision}

; Lifetime of Remember-Me Cookie in seconds
config.remember_me_cookie_lifetime = 1814400
config.cookie_prefix =
